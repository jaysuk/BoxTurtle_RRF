while iterations < global.AFC_total_lanes                                                  ; Loop through each lane using the built-in iteration counter
    M118 S{"Lane "^iterations^" length is "^global.AFC_lane_first_length[iterations]^"mm"} ; Output the stored 'first length' measurement for the current lane to the console